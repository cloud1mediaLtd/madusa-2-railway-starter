# Medusa Backend Environment Variables
# Copy this file to .env.local and update the values

# Node Environment
NODE_ENV=development

# Database
DATABASE_URL=postgres://postgres@localhost/medusa-db

# Redis (Optional - falls back to simulated redis)
REDIS_URL=redis://localhost:6379

# CORS Configuration
ADMIN_CORS=http://localhost:7000,http://localhost:9000,https://docs.medusajs.com
STORE_CORS=http://localhost:8000,https://docs.medusajs.com
AUTH_CORS=http://localhost:7000,http://localhost:9000,http://localhost:8000,https://docs.medusajs.com

# Secrets (Change these in production!)
JWT_SECRET=supersecret-local-dev
COOKIE_SECRET=supersecret-local-dev

# Backend URL
BACKEND_URL=http://localhost:9000

# Admin Credentials (for seeding)
MEDUSA_ADMIN_EMAIL=admin@example.com
MEDUSA_ADMIN_PASSWORD=supersecret

# Medusa Admin Settings
MEDUSA_ADMIN_ONBOARDING_TYPE=nextjs
MEDUSA_ADMIN_ONBOARDING_NEXTJS_DIRECTORY=storefront

# Email - Resend (Recommended)
# RESEND_API_KEY=your_resend_api_key
# RESEND_FROM_EMAIL=noreply@yourdomain.com

# Email - SendGrid (Alternative)
# SENDGRID_API_KEY=your_sendgrid_api_key
# SENDGRID_FROM=noreply@yourdomain.com

# Payment - Stripe
# STRIPE_API_KEY=your_stripe_secret_key
# STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# MinIO Storage Configuration (Optional - falls back to local storage)
# For local dev, leave commented to use local file storage
# MINIO_ENDPOINT=your-minio-endpoint.com
# MINIO_ACCESS_KEY=your-access-key
# MINIO_SECRET_KEY=your-secret-key
# MINIO_BUCKET=medusa-media

# Meilisearch Configuration (Optional)
# For local dev with Docker: docker run -d -p 7700:7700 -e MEILI_MASTER_KEY=masterKey getmeili/meilisearch:latest
# MEILISEARCH_HOST=http://localhost:7700
# MEILISEARCH_ADMIN_KEY=masterKey

# Worker Mode (Optional - for Railway deployment)
# MEDUSA_WORKER_MODE=shared # Options: shared | server | worker
# MEDUSA_DISABLE_ADMIN=false # Set to true to disable admin on worker instances
